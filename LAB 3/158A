#include <stdio.h>

int main()
{
	int n, k, p[50], c=0;

	scanf_s("%d %d", &n, &k);

	for (int i = 0; i < n; i++)
	{
		scanf_s("%d", &p[i]);

		if (p[i] && p[i] >= p[k-1]) 
			c++;
	}
	printf("%d", c);

	return 0;
}
